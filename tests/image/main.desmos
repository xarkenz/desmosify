var clicked: bool = false;

display {
    @image(
        @include_data("example.png"), // url
        "test image", // name
        (0, 0), // center
        5, // width
        5, // height
    ): click(action() { clicked := true });
}
